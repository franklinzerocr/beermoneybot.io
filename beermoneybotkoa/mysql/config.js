
const config = {
    // Start port
    port: 3001,

    // Database configuration
    database: {
      HOST     : 'localhost',
      USER     : 'root',
      PASSWORD : 'root',
      DATABASE : 'beermoney_users',
      PORT:'8889'
    }
  }

  module.exports = config
